#include <pebble.h>
#include "atest.h"
#include <string.h>


void atest()
{
  // char str[80] = "This is - www.tutorialspoint.com - website";
  // char  s[2] = "-";
  // char *token;
    APP_LOG(APP_LOG_LEVEL_DEBUG, "In atest");
   // get the first token 
   /*token = strtok(str, s);
   APP_LOG(APP_LOG_LEVEL_DEBUG, "%s",token);

   while( token != NULL ) 
   {       
      token = strtok(NULL, s);
	   APP_LOG(APP_LOG_LEVEL_DEBUG, "%s",token);
	   
   }
*/

}
